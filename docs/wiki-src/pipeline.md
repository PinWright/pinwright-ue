# pipeline

Automation bridge and build-pipeline tooling for the editor MCP server — inspect bridge status and run supported build tooling such as Unreal Build Tool. Reach for `call("system")` instead when you want top-level test runs, console commands, or subsystem UFunction calls; `pipeline.*` is scoped to the automation-bridge and build-tooling surface.
