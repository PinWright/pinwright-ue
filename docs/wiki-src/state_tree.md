# state_tree

Author `UStateTree` assets — add evaluators, tasks, and conditions, configure transition triggers, and bind state-tree properties to parameter / context sources. Use `call("behavior_tree")` for legacy AI behavior trees; state trees are the modern UE 5 AI/sequencing replacement and this namespace is scoped to their authoring graph.
